import React, { Component } from 'react';

const Jumbo = (props) => (
    <div className = "jumbotron">
        <h1>Memory Game</h1>
        <h2>Click on each image, but don't hit one twice!</h2>
    </div>
);

export default Jumbo